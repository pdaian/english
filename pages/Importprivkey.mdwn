**importprivkey** is an RPC command on [[bitcoind]] that allows a [[private_key]] to be added to a wallet.

This is a Bitcoin 0.6.0 feature and is available in the 0.6 release candidates and later.

<pre>
$ bitcoind help
[...]
  importprivkey <bitcoinprivkey> [label]
</pre>

## See also

* [[Sweepprivkey_proposal]]
