A **frontend** connects to a running [[client|tags/Clients]] (for example via JSON-RPC) to provide an alternative user interface for it or wrap it into an API usable with a different programming language, it does not implement the bitcoin p2p protocol itself.

[[!inline pages="tagged(Frontends)" actions=no archive=yes sort=title]]
