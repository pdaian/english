This category lists all pages that had been requested for speedy deletion. Users can comment on the request on the respective talk pages. Administrators can then delete the pages based on the feedback or remove the request for deletion.
To request for speedy deletion of an article add <pre>{{db}}</pre> in the article.

[[!inline pages="tagged(Candidates_for_speedy_deletion)" actions=no archive=yes sort=title]]
