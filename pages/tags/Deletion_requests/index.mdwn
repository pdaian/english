Deletion requests lists all pages that had been requested for deletion. Users can comment on the request on the respective talk pages. Admins can actually delete the pages based on the feedback or remove the request for deletion.
To request for deletion of an article add <pre>{{delete}}</pre> in the article.

[[!inline pages="tagged(Deletion_requests)" actions=no archive=yes sort=title]]
